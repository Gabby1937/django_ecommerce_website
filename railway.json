{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "echo $PORT && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn ecom.ecom.wsgi:application --bind 0.0.0.0:$PORT --log-level debug",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 20
  }
}