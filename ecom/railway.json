{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "rootDir": "ecom"
  },
  "deploy": {
    "startCommand": "cd ecom && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn ecom.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}